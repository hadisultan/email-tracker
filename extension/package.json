{
  "name": "@email-tracker/extension",
  "version": "0.0.1",
  "private": true,
  "description": "Manifest V3 Chrome extension that injects a tracking pixel into Gmail compose and beacons self-views.",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "package": "npm run build && node scripts/package.mjs",
    "test": "vitest run --project extension"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.280",
    "archiver": "^7.0.1",
    "typescript": "^5.6.3"
  }
}
